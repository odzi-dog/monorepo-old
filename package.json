{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "build": "npm run bootstrap && lerna run build",
    "build:cloud/api": "npm run bootstrap && lerna run build --scope @odzi-cloud/api"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}

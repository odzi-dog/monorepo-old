{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "build": "npm run bootstrap && lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
